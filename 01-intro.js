const amount = 12;

if (amount < 10) {
    console.log('small number');
} else {
    console.log('large number');
}

console.log(`First node app!`);

// CLI: Save the JS code in a file, and run the file through command line by typing `node filename.js`